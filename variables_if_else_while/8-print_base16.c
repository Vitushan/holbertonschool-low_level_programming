#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
	int hexa = 15;
		
		for(hexa = 0; hexa <= 15; hexa++)
		{
			putchar(hexa);
		}
	putchar('\n');
	return (0);
}
