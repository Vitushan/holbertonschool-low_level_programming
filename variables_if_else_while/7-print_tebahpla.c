#include <stdio.h>

 /**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alpha = 'a';
        int i = 0;
	
	for (alpha ='a'; alpha >= 'z'; alpha++)
	{
		putchar(alpha);
	}
	putchar('\n');
		return (0);
}
