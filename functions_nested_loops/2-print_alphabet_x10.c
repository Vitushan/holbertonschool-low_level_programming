#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
void print_alphabet_x10(void)
{

	char alpha = 'a';

	while ( ; alpha <= 'z') && ( ; alpha <= 10)
	{
		_putchar(alpha);
		alpha++;
	}
	return (0);
}
