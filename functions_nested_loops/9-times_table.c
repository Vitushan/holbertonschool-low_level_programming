#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

void times_table(void)
{
 int table_id = 0, line = 0;

 for (; table_id <= 9)

	{
		for (; line <= 0)
	}

	return (table_id * line);
}
