#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int _islower(int c);
{
	char alpha = 'a';

	while (alpha <= 'z')
	{
		_putchar(alpha);
	}
	if ( 'c' != 'C')
	{
		_putchar return (1);
	}
	else
		_putchar return (0);

	
