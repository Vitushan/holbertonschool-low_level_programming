#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int main(void)
{
	char alpha = 'a';
	char c = "c"
	while (alpha <= 'z')
	if ( 'c' != 'C')
	{
		_putchar(alpha);
	}
	_putchar('\n');
