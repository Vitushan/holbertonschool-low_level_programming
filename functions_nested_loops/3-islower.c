#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int _islower(int c);
{
	char alpha = 'a';
	char c = 'c'
	while (alpha <= 'z')
	{
		_putchar(alpha);
	}
	if ( 'C' != c)
	{
		_putchar return (1);
	}
	else
		_putchar return (0);
	_putchar('\n');
	
