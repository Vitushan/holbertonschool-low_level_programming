#include <stdio.h>

int main (void)
{
    char lowcase = 'a', lowcase2 = 'z';

    
    if (lowcase < lowcase2 )
    {
    for(; lowcase < lowcase2; lowcase++ )
    
    putchar('\n');
    return (0);
    }
}
