#include <stdio.h> //bibliotheque
int main(void) // main principal
{
  puts("\"Programming is like building a multilingual puzzle");//affiche message a l'ecran
  return (0); //renvoie 0 si compiler avec succés
}
